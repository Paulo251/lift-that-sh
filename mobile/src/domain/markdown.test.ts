import { describe,expect,it } from 'vitest';import { newWorkout } from './factories';import { parseWorkout,serializeWorkout,DocumentError } from './markdown'
const exercise={id:'e',name:'E',description:null,primary_muscle_group:'chest' as const,secondary_muscle_groups:[],equipment:'barbell' as const,category:'compound' as const}
describe('Markdown',()=>{it('preserva integralmente o corpo no round-trip',()=>{const w={...newWorkout('A'),body:'\n# Notas\n\ntexto  \n'};expect(parseWorkout(serializeWorkout(w)).body).toBe(w.body)});it('rejeita YAML inválido',()=>expect(()=>parseWorkout('---\n: [\n---\n')).toThrow(DocumentError));it('rejeita versão futura',()=>{const raw=serializeWorkout(newWorkout('A')).replace('schema_version: 1','schema_version: 2');expect(()=>parseWorkout(raw)).toThrow()});it('rejeita ids de entidades duplicados',()=>{const w=newWorkout('A');w.exercises=[{id:w.id,exercise,position:0,target_sets:1,target_reps:1,notes:null}];expect(()=>parseWorkout(serializeWorkout(w))).toThrow(/duplicados/)});it('aceita duas ocorrências independentes do mesmo snapshot',()=>{const w=newWorkout('A');w.exercises=[{id:'a',exercise,position:0,target_sets:3,target_reps:8,notes:null},{id:'b',exercise:structuredClone(exercise),position:1,target_sets:4,target_reps:12,notes:'outra meta'}];expect(parseWorkout(serializeWorkout(w)).exercises).toHaveLength(2)})})
