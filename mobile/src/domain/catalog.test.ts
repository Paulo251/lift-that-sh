import {describe,expect,it} from 'vitest';import {filterExercises,normalizeSearch} from './catalog';import type {Exercise} from './models'
const xs:Exercise[]=[{id:'1',name:'Elevação Lateral',description:null,primary_muscle_group:'shoulders',secondary_muscle_groups:[],equipment:'dumbbell',category:'isolation',custom:false},{id:'2',name:'Supino pessoal',description:'Peito',primary_muscle_group:'chest',secondary_muscle_groups:['triceps'],equipment:'barbell',category:'compound',custom:true}]
describe('catálogo',()=>{it('busca sem acentos',()=>expect(normalizeSearch('ELEVAÇÃO')).toBe('elevacao'));it('combina todos os filtros',()=>expect(filterExercises(xs,{query:'pêito',muscle:'triceps',equipment:'barbell',category:'compound',origin:'custom'}).map(x=>x.id)).toEqual(['2']))})
